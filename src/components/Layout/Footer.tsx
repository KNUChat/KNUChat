import styled from "styled-components";

const Footer = () => {
  return (
    <FooterWrapper>
      <div>Footer</div>
    </FooterWrapper>
  );
};

export default Footer;

const FooterWrapper = styled.div`
  display: flex;
  height: 6rem;
`;
