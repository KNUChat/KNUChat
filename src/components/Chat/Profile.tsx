import styled from "styled-components";


const Profile = () => {

    return (
        <ProfileWrapper>
            
        </ProfileWrapper>
    );
}

export default Profile;

const ProfileWrapper = styled.div`
    text-align: center;
`;