import styled from "styled-components";

const ChatPrintBox = () => {
    return (
      <ChatPrintWrapper>
        
      </ChatPrintWrapper>
    );
  };
  
  export default ChatPrintBox;

  const ChatPrintWrapper = styled.div`
  
  `;