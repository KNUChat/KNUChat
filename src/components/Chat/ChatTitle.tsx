import styled from "styled-components";

const ChatTitle = () => {
    return (
        <ChatTitleWrapper>
            ChatRoom Title
        </ChatTitleWrapper>
    );
};

export default ChatTitle;

const ChatTitleWrapper = styled.div`
    text-align: center;
`;